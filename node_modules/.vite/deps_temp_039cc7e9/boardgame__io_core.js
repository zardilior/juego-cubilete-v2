import {
  ActivePlayers,
  GameMethod,
  INVALID_MOVE,
  Stage,
  TurnOrder,
  require_lodash
} from "./chunk-V2WRADDA.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/boardgame.io/dist/esm/core.js
var import_lodash = __toESM(require_lodash());
var PlayerView = {
  /**
   * STRIP_SECRETS
   *
   * Reducer which removes a key named `secret` and
   * removes all the keys in `players`, except for the one
   * corresponding to the current playerID.
   */
  STRIP_SECRETS: ({ G, playerID }) => {
    const r = { ...G };
    if (r.secret !== void 0) {
      delete r.secret;
    }
    if (r.players) {
      r.players = playerID ? {
        [playerID]: r.players[playerID]
      } : {};
    }
    return r;
  }
};
export {
  ActivePlayers,
  GameMethod,
  INVALID_MOVE,
  PlayerView,
  Stage,
  TurnOrder
};
//# sourceMappingURL=boardgame__io_core.js.map
